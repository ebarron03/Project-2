version: "3.8"

services:

  hq: 
    build: ./hq
    container_name: hq
    ports:
      - "8081:80"

  cloud:
    build: ./cloud
    container_name: cloud
    ports:
      - "8082:80"

  security:
    build: ./security
    container_name: security
    ports:
      - "8083:80"

  customer:
    build: ./customer
    container_name: customer
    ports:
      - "8084:80"

  research:
    build: ./research
    container_name: research
    ports:
      - "8085:80"
